// Esse arquivo vai pegar todos os códigos do JavaScript
var contador = 0;
function converterTemp() {
    // Forma de busca de uma parte do HTML por meio do ID
    // innerText = inserir um texto
    contador++;
    document.getElementById("saída").innerText = contador;
}
